# Minimal dependencies for Vercel Serverless Backend (Total < 500MB)

# --- Backend API ---
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# --- Google Gemini ---
google-genai>=0.3.0
google-generativeai>=0.7.0

# --- Vector Store ---
chromadb>=0.5.0
pysqlite3-binary>=0.5.0

# --- Environment & Utils ---
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.26.0

# Note: We specifically EXCLUDE playwright, spacy, and streamlit here 
# because they inflate the Vercel Lambda bundle over the 500MB hard limit.
# Those heavy packages are only needed locally during the Ingestion/Processing phases.
